三、多选题
<br>
<ol>
    <% topic.forEach(function(item){ %>
        <li>
            <%= item.question%><br>
            <% item.options.forEach(function(element,i){ %>
                <input type="checkbox" name="<%= item.name%>" value="<%= element.key%>"><%= element.key%>、<%= element.val%><br>
            <%})%>
        </li>
    <%})%>
</ol>
