四、判断题
<br>
<ol>
    <% topic.forEach(function(item){ %>
        <li>
            <%= item.question%><br>
            <% item.options.forEach(function(element,i){ %>
                <input type="radio" name="<%= item.name%>" value="<%= element.key%>"
                <% if(item.value===element.key){ %> checked="checked" <% } %>><%= element.val%><br>
            <%})%>
        </li>
    <%})%>
</ol>
